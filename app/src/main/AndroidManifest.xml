<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.csc530.familytree">
	
	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:label="@string/app_name"
		android:theme="@style/Theme.FamilyTree">
		<activity
			android:name=".controllers.LaunchActivity"
			android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity
			android:name=".controllers.AuthActivity"
			android:exported="false" />
		<activity
			android:name=".controllers.LoadTreeActivity"
			android:exported="false" />
		<activity
			android:name=".controllers.TreeActivity"
			android:exported="false" />
	</application>

</manifest>